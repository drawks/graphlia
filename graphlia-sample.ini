[amqp]
host = localhost
user = guest
pass = guest
vhost = /
exchange = stats

[gmond]
multicast = True
ip = 239.2.11.71
port = 8649

[mapping]
default = ganglia.${hostname}.${GROUP}.${metricname}
this.metric.needs_tobe_fixed = ${metricname}.${hostname}
